# A TOML linter such as https://github.com/tombi-toml/tombi can use this schema to validate your config.
# If you encounter any issues, please make an issue at https://github.com/yazi-rs/schemas.

#:schema https://yazi-rs.github.io/schemas/yazi.json

[mgr]
show_hidden    = true

[opener]
neovim = [
  { run = 'nvim "$@"', desc = "Open in Neovim", block = true, orphan = false }
]
firefox = [
  { run = 'firefox "$@"', desc = "Open in Firefox", orphan = true }
]

[open]
rules = [
  { mime = "text/html", use = ["neovim", "firefox"] },
]
